# 🧮 Intrinsic Value Calculator

A lightweight web app built with Vite + React + TypeScript, using Zustand for state management, TailwindCSS and shadcn/ui for styling, and Biome for formatting/linting.
The goal of this project is to calculate the intrinsic value of a stock based on financial inputs.

## 🚀 Tech Stack

- Vite — fast dev/build tooling

- React + TypeScript — component-based UI with strong typing

- Zustand — simple and scalable global state

- TailwindCSS — utility-first styling

- shadcn/ui — accessible, customizable UI components

- Biome — formatter, linter, and static analysis
